name: Samy EKS Automated Pipeline

on:
  push:
    branches:
      - main

jobs:
  Build-and-Deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout Code
    - name: Checkout source code
      uses: actions/checkout@v3

    # 2. Setup Node.js & Run Tests
    - name: Setup Node.js Environment
      uses: actions/setup-node@v3
      with:
        node-version: 18

    - name: Install Dependencies & Run Tests
      run: |
        npm install
        npm test -- --watchAll=false --passWithNoTests

    # 3. Configure AWS Credentials Securely
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

    # 4. Login to Amazon ECR
    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    # 5. Build, Tag, and Push Docker Image to ECR
    - name: Build, Tag, and Push Image to ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: b17-ecr-repo
        IMAGE_TAG: latest
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

    # 6. Update EKS Kubeconfig
    - name: Update Kubeconfig for EKS
      run: |
        aws eks update-kubeconfig --name MyEKS --region ap-south-1

    # 7. Deploy using Token to Bypass local CLI authentication errors
    - name: Deploy to AWS EKS
      run: |
        # AWS CLI se dynamic bearer token generate karna
        TOKEN=$(aws eks get-token --cluster-name MyEKS | jq -r '.status.token')
        
        # Token use karke manifests apply karna
        kubectl apply -f k8s/deployment.yml --token=$TOKEN
        kubectl apply -f k8s/service.yml --token=$TOKEN
        kubectl rollout restart deployment/samy-react-app --token=$TOKEN